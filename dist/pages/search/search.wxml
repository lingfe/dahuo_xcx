<view class="page bg">
    <!--  搜索栏  start   -->
    <view class="page__bd">
        <view class="weui-search-bar" style="background-color:#fff;padding:10px 10px;">
            <view class="bg_search">
                <view class="weui-search-bar__box ">
                    <icon class="weui-icon-search_in-box" type="search" size="14"></icon>
                    <input type="text" class="weui-search-bar__input" bindinput="bindinputValue" placeholder="请输入城市名称或字母" value="{{inputVal}}" focus="{{inputShowed}}" bindinput="inputTyping" />
                    <view class="weui-icon-clear" wx:if="{{inputVal.length > 0}}" bindtap="clearInput">
                        <icon type="clear" size="14"></icon>
                    </view>
                </view>
            </view>
        </view>
        <!-- 关键字  start -->
        <view class="weui-cells searchbar-result bg " wx:if="{{inputVal.length >= 0}}">
            <view class="weui-cells__title">当前城市:</view>
            <view class="weui-cell weui-cell_active weui-cell_bd bg stop_1" >
              <view class="weui-btn mini-btn bg_b"  bindtap="bindtapGetAddres" data-info="{{addressInfo}}"  size="mini">
                <view class="bg_b_img"><image src="/images/address.png"></image></view>
                <view class="bg_b_text">{{addressInfo}}</view>
              </view>
            </view>
            <view wx:for="{{address}}" wx:key="{{*this}}">
              <view class="weui-cells__title ">{{item.letter}}</view>
                  <view class="weui-cell__bd" bindtap="bindtapGetAddres"  wx:for="{{item.info}}" wx:key="{{*this}}" data-info="{{item}}" class="weui-cell" hover-class="weui-cell_active">
                      <view>{{item}}</view>     
                  </view>
            </view>
        </view>
        <!--  关键字  end   -->
    </view>
    <!--  搜索栏  end   -->
</view>